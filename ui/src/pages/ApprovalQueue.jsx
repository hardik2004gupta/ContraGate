/**
 * ApprovalQueue — landing page showing all pending approval requests.
 * PHASE 0 SCAFFOLD — implement in Phase 7.
 *
 * Displays (CLAUDE.md §17):
 *   - All pending requests sorted by time remaining before 30-minute timeout
 *   - Each card: operation intent summary, risk tier badge, primary table,
 *     estimated rows, time remaining
 *   - Clicking a card opens ContractView
 */
