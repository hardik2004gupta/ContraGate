/**
 * ContraGate React App Root
 * PHASE 0 SCAFFOLD — implement in Phase 7.
 *
 * Three views (CLAUDE.md §17):
 *   /           → ApprovalQueue  (landing page — pending approvals sorted by timeout)
 *   /approval/:id → ContractView  (full consequence contract with decision panel)
 *   /audit      → AuditLog       (completed operations with blast radius accuracy deltas)
 */
